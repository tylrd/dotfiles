# ~/.tmuxinator/ephemeral.yml

<% require 'socket'; host = Socket.gethostname %>
<% if host == 'taylor-home.local' %>
    <% workspace = '~/Documents/code' %>
<% elsif host.start_with?('bc-') %>
    <% workspace = '~/productivity/eph' %>
<% else %>
    <% p 'hostname not recognized, defaulting to ~/Documents/code' %>
    <% workspace = '~/Documents/code' %>
<% end %>

name: ephemeral
root: <%= workspace %>
windows:
  - terraform:
      root: <%= workspace %>/platform-terraform-lib
      layout: even-horizontal
      panes:
        - vim .
        - terraform:
          - SSH_USER=taylor.daugherty source bin/env
          - clear
          - git fetch origin
  - chef:
      root: <%= workspace %>/platform-chef-repo
      layout: even-horizontal
      panes:
        - vim .
        - chef:
          - cook
          - clear
          - git fetch origin
  - cookbooks:
      root: <%= workspace %>/platform-cookbooks
      layout: even-horizontal
      panes:
        - vim .
        - chef:
          - cook
          - clear
          - git fetch origin
  - packer:
      root: <%= workspace %>/platform-packer-lib
      layout: even-horizontal
      panes:
        - vim .
        - packer:
          - git fetch origin
